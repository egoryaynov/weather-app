const initialState = {
    // ARRAY OF OBJECTS
    forecasts: [],
    isFetching: false
}

const forecastWeatherReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default forecastWeatherReducer;