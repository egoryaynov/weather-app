const initialState = {
    currentCity: null,
    isFetching: false
}

const currentWeatherReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default currentWeatherReducer;