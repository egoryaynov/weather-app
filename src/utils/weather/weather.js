export const getWeatherFromAPIByID = (cityID) => {
    if (data.cod === 200) {

    } else {
        return data.message
    }
}
export const getWeatherFromAPIByCity = (city) => {
    if (data.cod === 200) {

    } else {
        return data.message
    }
}